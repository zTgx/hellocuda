# HelloCUDA

## Pre-require
* set CUTALASSPATH
```bash
export CUTALASSPATH=/path/to/cutalass
```

* set LIBTORCH_HOME
```bash
export LIBTORCH_HOME=/path/to/libtorch
```

## Build

```bash
make build
cd build
cmake ..
make
```
